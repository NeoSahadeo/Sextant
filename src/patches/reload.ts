export default () => {
	// const clone = document.body.parentNode?.cloneNode(true);
	// if (document.body.parentNode && clone) {
	// 	document.getElementsByTagName("html")[0].remove();
	// 	// document.appendChild(clone);
	// }
	// const stop_reload = () => {
	// 	document.addEventListener("keydown", (event) => {
	// 		window.logger("Ctrl+R is disabled.");
	// 		// Check if Ctrl (or Command on macOS) key is pressed along with R
	// 		if (
	// 			(event.ctrlKey || event.metaKey) &&
	// 			(event.key === "r" || event.key === "R")
	// 		) {
	// 			event.preventDefault(); // Prevent the default reload action
	// 			window.logger("Ctrl+R is disabled.");
	// 		}
	// 	});
	// };
	//
	// try {
	// 	document.removeEventListener("keydown", stop_reload);
	// 	stop_reload();
	// } catch {
	// 	stop_reload();
	// }
};
